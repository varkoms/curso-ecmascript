// Forma comun de crear una function en JS

function square(num) {
  return num * num;
}

// Arrow functions
const square = (num) => {
  return num * num;
}

// Arrow function reducida
const square = num => num * num;